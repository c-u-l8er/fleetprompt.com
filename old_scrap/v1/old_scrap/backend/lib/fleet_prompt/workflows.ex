defmodule FleetPrompt.Workflows do
  @moduledoc """
  Ash domain for workflow-related resources.

  This is currently a placeholder so the application can boot and Ash domain
  verification can succeed while workflow resources are implemented in later
  phases.
  """

  use Ash.Domain

  resources do
    # Workflow resources will be added in Phase 4 (e.g. Workflow, WorkflowRun).
  end
end
