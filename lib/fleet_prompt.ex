defmodule FleetPrompt do
  @moduledoc """
  FleetPrompt — the open agent marketplace for the [&] Protocol ecosystem.

  FleetPrompt is the registry where production-ready AI agents are published,
  discovered, and deployed. Every listed agent carries a versioned manifest,
  a computed trust score, declared permissions, and a linked SpecPrompt spec.
  """
end
